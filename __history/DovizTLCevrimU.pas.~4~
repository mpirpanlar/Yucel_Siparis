unit DovizTLCevrimU;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics,
  Controls, Forms, uniGUITypes, uniGUIAbstractClasses,
  uniGUIClasses, uniGUIForm, uniButton, uniEdit, uniGUIBaseClasses, uniPanel,
  uniLabel;

type
  TfrmDovizTLCevrim = class(TUniForm)
    UniPanel1: TUniPanel;
    txtTLFiyat: TUniEdit;
    UniButton1: TUniButton;
    lblStokKodu: TUniLabel;
    txtDovizFiyat: TUniEdit;
    txtKur: TUniEdit;
    procedure UniButton1Click(Sender: TObject);
    procedure UniFormShow(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
    FisDetayID : Integer;
    StokKodu : String;
    DovizFiyat, Kur, TLFiyat :Real;
  end;

function frmDovizTLCevrim: TfrmDovizTLCevrim;

implementation

{$R *.dfm}

uses
  MainModule, uniGUIApplication, SiparisU, SiparisDMU;

function frmDovizTLCevrim: TfrmDovizTLCevrim;
begin
  Result := TfrmDovizTLCevrim(UniMainModule.GetFormInstance(TfrmDovizTLCevrim));
end;

procedure TfrmDovizTLCevrim.UniButton1Click(Sender: TObject);
begin
  frmSiparisDM.qFisDetay.Edit;

    if frmSiparisDM.qFisDetay.FieldByName('ParaBirimi').Value = '0' then
      begin
        frmSiparisDM.qFisDetay.FieldByName('BirimFiyat').AsFloat := StrToFloat(txtTLFiyat.Text); /// Burasý Fatih e sorulacak. Value alýnýyordu...
      end else
      begin
        frmSiparisDM.qFisDetay.FieldByName('BirimFiyat').AsFloat := StrToFloat(txtDovizFiyat.Text);
      end;

  frmSiparisDM.qFisDetay.Post;
end;

procedure TfrmDovizTLCevrim.UniFormShow(Sender: TObject);
begin
  lblStokKodu.Caption := StokKodu;
  txtDovizFiyat.Text := FloatToStr(DovizFiyat);
  txtKur.Text := FloatToStr(Kur);
  txtTLFiyat.Text := FloatToStr(TLFiyat);

end;

end.
